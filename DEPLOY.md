# 🚀 Инструкция по развертыванию бота на VDS

Пошаговая инструкция по установке и запуску Telegram бота на VDS сервере.

## 📋 Подготовка

### 1. Подготовка проекта для GitHub

**ВАЖНО:** Перед загрузкой на GitHub убедитесь, что в `config.py` нет реальных токенов!

1. **Создайте репозиторий на GitHub:**
   ```bash
   # Инициализируйте git (если еще не сделано)
   git init
   
   # Добавьте все файлы
   git add .
   
   # Сделайте первый коммит
   git commit -m "Initial commit"
   
   # Создайте репозиторий на GitHub и добавьте remote
   git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   git branch -M main
   git push -u origin main
   ```

2. **Перед коммитом убедитесь, что:**
   - В `config.py` нет реальных токенов (используйте `config.py.example`)
   - Файл `.gitignore` правильно настроен
   - База данных `bot_database.db` не попадает в репозиторий

### 2. Подключение к VDS

Подключитесь к вашему VDS через SSH:
```bash
ssh root@ВАШ_IP_АДРЕС
# или
ssh username@ВАШ_IP_АДРЕС
```

## 🔧 Установка на VDS

### Шаг 1: Обновление системы

```bash
# Для Ubuntu/Debian
sudo apt update && sudo apt upgrade -y

# Для CentOS/RHEL
sudo yum update -y
```

### Шаг 2: Установка Python и зависимостей

```bash
# Установка Python 3.10+ и pip
sudo apt install python3 python3-pip python3-venv git -y

# Проверка версии Python
python3 --version  # Должно быть 3.10 или выше
```

### Шаг 3: Создание пользователя для бота (рекомендуется)

```bash
# Создаем пользователя
sudo useradd -m -s /bin/bash botuser

# Переключаемся на пользователя
sudo su - botuser
```

### Шаг 4: Клонирование репозитория

```bash
# Клонируем репозиторий
cd ~
git clone https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git bot_logi
cd bot_logi
```

### Шаг 5: Настройка виртуального окружения

```bash
# Создаем виртуальное окружение
python3 -m venv venv

# Активируем его
source venv/bin/activate

# Устанавливаем зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 6: Настройка конфигурации

```bash
# Копируем пример конфига
cp config.py.example config.py

# Редактируем config.py
nano config.py
# или
vim config.py
```

**В `config.py` укажите:**
```python
BOT_TOKEN = "ВАШ_ТОКЕН_БОТА"  # Получите у @BotFather
ADMIN_IDS = [ВАШ_TELEGRAM_ID]  # Ваш Telegram ID
CRYPTOBOT_TOKEN = "ВАШ_ТОКЕН"  # Опционально
REQUIRED_CHANNEL_ID = None  # Опционально
```

### Шаг 7: Создание необходимых директорий

```bash
# Директории создадутся автоматически при первом запуске,
# но можно создать вручную:
mkdir -p logs uploads
```

### Шаг 8: Первый запуск (для инициализации БД)

```bash
# Активируем виртуальное окружение (если еще не активировано)
source venv/bin/activate

# Запускаем бота для инициализации БД
python3 main.py

# Нажмите Ctrl+C для остановки
```

## 🔄 Настройка автозапуска через systemd

### Создание systemd service файла

```bash
# Выйдите из виртуального окружения
exit

# Создаем service файл (от root или через sudo)
sudo nano /etc/systemd/system/bot-logi.service
```

**Содержимое файла `/etc/systemd/system/bot-logi.service`:**
```ini
[Unit]
Description=Telegram Bot Logi
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/bot_logi
Environment="PATH=/home/botuser/bot_logi/venv/bin"
ExecStart=/home/botuser/bot_logi/venv/bin/python3 /home/botuser/bot_logi/main.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**ВАЖНО:** Замените `/home/botuser/bot_logi` на ваш реальный путь!

### Активирование сервиса

```bash
# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable bot-logi.service

# Запускаем сервис
sudo systemctl start bot-logi.service

# Проверяем статус
sudo systemctl status bot-logi.service
```

### Управление сервисом

```bash
# Запуск
sudo systemctl start bot-logi

# Остановка
sudo systemctl stop bot-logi

# Перезапуск
sudo systemctl restart bot-logi

# Просмотр логов
sudo journalctl -u bot-logi -f

# Просмотр последних 100 строк логов
sudo journalctl -u bot-logi -n 100
```

## 📝 Полезные команды

### Просмотр логов бота

```bash
# Логи systemd
sudo journalctl -u bot-logi -f

# Логи из файла (если настроено)
tail -f /home/botuser/bot_logi/logs/bot.log
```

### Обновление кода

```bash
# Переключаемся на пользователя бота
sudo su - botuser

# Переходим в директорию проекта
cd ~/bot_logi

# Получаем обновления
git pull origin main

# Активируем виртуальное окружение
source venv/bin/activate

# Обновляем зависимости (если нужно)
pip install -r requirements.txt

# Выходим
exit

# Перезапускаем сервис
sudo systemctl restart bot-logi
```

### Резервное копирование базы данных

```bash
# Создаем директорию для бэкапов
mkdir -p ~/backups

# Копируем БД
cp /home/botuser/bot_logi/bot_database.db ~/backups/bot_database_$(date +%Y%m%d_%H%M%S).db

# Автоматический бэкап (можно добавить в crontab)
# crontab -e
# Добавьте строку: 0 2 * * * cp /home/botuser/bot_logi/bot_database.db ~/backups/bot_database_$(date +\%Y\%m\%d_\%H\%M\%S).db
```

## 🔒 Безопасность

1. **Не храните токены в репозитории:**
   - Используйте `config.py.example` как шаблон
   - Добавьте `config.py` в `.gitignore`

2. **Настройте файрвол:**
   ```bash
   # Ubuntu/Debian
   sudo ufw allow 22/tcp  # SSH
   sudo ufw enable
   ```

3. **Используйте SSH ключи вместо паролей**

4. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте логи:
   ```bash
   sudo journalctl -u bot-logi -n 50
   ```

2. Проверьте токен в `config.py`

3. Проверьте права доступа:
   ```bash
   sudo chown -R botuser:botuser /home/botuser/bot_logi
   ```

### Бот падает с ошибкой

1. Проверьте, что все зависимости установлены:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

2. Проверьте версию Python:
   ```bash
   python3 --version  # Должно быть 3.10+
   ```

### Проблемы с базой данных

1. Проверьте права на файл БД:
   ```bash
   ls -la bot_database.db
   sudo chown botuser:botuser bot_database.db
   ```

2. Пересоздайте БД (⚠️ потеряете данные):
   ```bash
   rm bot_database.db
   python3 main.py  # Создаст новую БД
   ```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `sudo journalctl -u bot-logi -f`
2. Проверьте файл логов: `tail -f logs/bot.log`
3. Убедитесь, что все зависимости установлены
4. Проверьте конфигурацию в `config.py`

---

**Готово!** Ваш бот должен работать на VDS. Проверьте его работу, отправив команду `/start` в Telegram.

