# Инструкция по настройке и запуску

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Откройте файл `config.py` и настройте следующие параметры:

```python
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"  # Получите у @BotFather
ADMIN_IDS = [123456789, 987654321]  # Ваши Telegram ID
CRYPTOBOT_TOKEN = "your_token"  # Опционально, для платежей
REQUIRED_CHANNEL_ID = None  # ID канала для подписки (опционально)
```

**Как получить BOT_TOKEN:**
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**Как узнать свой Telegram ID:**
1. Найдите @userinfobot в Telegram
2. Отправьте любое сообщение
3. Бот вернет ваш ID

**Как получить CRYPTOBOT_TOKEN:**
1. Найдите @CryptoBot в Telegram
2. Отправьте `/start`
3. Перейдите в настройки API
4. Создайте токен и скопируйте его

### 3. Запуск бота

```bash
python main.py
```

Бот запустится и начнет работать в режиме long polling.

## Первоначальная настройка

После первого запуска:

1. **База данных создастся автоматически**
2. **Создадутся дефолтные кнопки и настройки**
3. **Настройте админ-панель:**
   - Отправьте `/admin` или нажмите кнопку "🔐 Админ-панель"
   - Создайте категории и подкатегории
   - Создайте позиции товаров
   - Загрузите товары (файлы или .txt с логами)

## Структура товаров

### Строковые товары (логи):
1. Создайте позицию с типом "string"
2. Загрузите .txt файл через админ-панель
3. Каждая строка файла = один товар
4. При покупке пользователь получает указанное количество строк

### Файловые товары:
1. Создайте позицию с типом "file"
2. Загрузите файл через админ-панель
3. При покупке пользователь получает файл целиком

## Важные замечания

- Регулярно делайте резервные копии файла `bot_database.db`
- Проверяйте логи в папке `logs/`
- Загруженные файлы хранятся в папке `uploads/`
- Для работы с CryptoBot нужен активный токен

## Решение проблем

**Бот не запускается:**
- Проверьте правильность токена
- Убедитесь, что установлены все зависимости
- Проверьте логи в `logs/bot.log`

**Платежи не работают:**
- Проверьте токен CryptoBot
- Убедитесь, что токен активен
- Проверьте баланс в CryptoBot

**Товары не загружаются:**
- Проверьте формат файла (.txt для строковых товаров)
- Убедитесь, что файл не пустой
- Проверьте права доступа к папке `uploads/`

