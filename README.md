# Telegram Bot - Магазин логов

Полнофункциональный Telegram-бот для продажи логов (строк и файлов) с интеграцией CryptoBot для платежей.

## 🚀 Возможности

- **Магазин товаров**: Категории → Подкатегории → Позиции → Товары
- **Два типа товаров**: Строковые (логи) и файловые
- **Платежи**: Интеграция с CryptoBot (USDT)
- **Админ-панель**: Полное управление ботом через удобный интерфейс
- **Промокоды**: Система промокодов для пополнения баланса
- **Логирование**: Полное логирование всех действий
- **Экспорт данных**: Экспорт статистики и пользователей в CSV
- **Канал-подписка**: Проверка подписки на канал перед использованием
- **Тех. работы**: Режим технических работ с кастомным текстом

## 📋 Требования

- Python 3.10+
- Telegram Bot Token
- CryptoBot Token (опционально, для платежей)

## 🔧 Установка

### Локальная установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте конфигурацию:
   - Скопируйте `config.py.example` в `config.py`
   - Отредактируйте `config.py` и укажите:
     - `BOT_TOKEN` - токен вашего Telegram бота
     - `ADMIN_IDS` - список ID администраторов (через запятую)
     - `CRYPTOBOT_TOKEN` - токен CryptoBot (если используете)
     - `REQUIRED_CHANNEL_ID` - ID канала для подписки (опционально)

4. Запустите бота:
```bash
python main.py
```

### Развертывание на VDS

📖 **Подробная инструкция по развертыванию на VDS сервере:** [DEPLOY.md](DEPLOY.md)

**Быстрый старт:**
1. Загрузите проект на VDS (через GitHub или напрямую)
2. Запустите скрипт развертывания:
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   ```
3. Настройте `config.py` с вашими токенами
4. Настройте systemd для автозапуска (см. [DEPLOY.md](DEPLOY.md))

## 📁 Структура проекта

```
bot_logi/
├── main.py                 # Главный файл бота
├── config.py               # Конфигурация
├── database.py             # Модели БД
├── keyboards.py            # Клавиатуры
├── utils.py                # Утилиты
├── handlers/
│   ├── __init__.py
│   ├── user_handlers.py    # Обработчики пользователей
│   └── admin_handlers.py   # Обработчики админов
├── requirements.txt        # Зависимости
├── README.md              # Документация
├── uploads/               # Загруженные файлы (создается автоматически)
└── logs/                  # Логи (создается автоматически)
```

## 🎮 Использование

### Для пользователей:

1. Запустите бота командой `/start`
2. Используйте кнопки главного меню для навигации
3. Выберите категорию → подкатегорию → товар
4. Пополните баланс через CryptoBot
5. Совершите покупку

### Для администраторов:

1. Используйте команду `/admin` или кнопку "🔐 Админ-панель"
2. Управляйте ассортиментом, пользователями, настройками
3. Загружайте товары через админ-панель
4. Просматривайте статистику и экспортируйте данные

## 🔐 Админ-панель

Админ-панель включает:

- **Статистика**: Пользователи, покупки, выручка, остатки
- **Платежка**: Настройка CryptoBot, просмотр платежей
- **Ответы бота**: Редактирование всех текстовых сообщений
- **Кнопки**: Управление главными кнопками меню
- **Ассортимент**: Создание/редактирование категорий, подкатегорий, позиций
- **Загрузка товаров**: Загрузка .txt файлов (для строк) или файлов (для файловых товаров)
- **Пользователи**: Поиск, изменение баланса, блокировка
- **Рассылка**: Массовая рассылка сообщений
- **Канал**: Настройка канала-подписки
- **Тех. работы**: Включение/выключение режима тех. работ
- **Промокоды**: Создание и управление промокодами
- **Уведомления**: Настройка уведомлений админу

## 💾 База данных

Используется SQLite (файл `bot_database.db`). База данных создается автоматически при первом запуске.

## 📝 Логирование

Все действия логируются в:
- Базу данных (таблица `logs`)
- Файл `logs/bot.log`

## 🔄 Обновления

Бот автоматически проверяет платежи каждые 30 секунд и зачисляет средства на баланс пользователя.

## ⚠️ Важно

- Убедитесь, что у бота есть права на отправку сообщений и файлов
- Для работы с CryptoBot нужен активный токен
- Регулярно делайте резервные копии базы данных

## 📄 Лицензия

Проект создан для личного использования.

## 🆘 Поддержка

При возникновении проблем проверьте:
1. Правильность токенов в `config.py`
2. Логи в файле `logs/bot.log`
3. Права доступа к файлам и директориям

